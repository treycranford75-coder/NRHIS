# Build071 Release Notes

Build071 permanently removes the recurring manual pull-request creation step. Successful builds now create or reuse a pull request into `develop`, return its URL, and stop only while waiting for CI and merge.
