Build096 hardens the finalized NRHIS historical query CLI. Direct Python execution now resolves the repository `src` package automatically, and PowerShell query arrays survive the `powershell.exe -File` boundary without collapsing multiple station or parameter values into one filter. No historical archive data is changed and the query path remains local-only with zero USGS requests.
