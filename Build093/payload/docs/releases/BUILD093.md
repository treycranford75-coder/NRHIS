# NRHIS Sprint 2 Build093

Build093 fixes the self-contained lifecycle installer-archive guard so a successful build can complete post-merge archive and receipt closeout without a PowerShell `PathType` parameter-binding failure.
