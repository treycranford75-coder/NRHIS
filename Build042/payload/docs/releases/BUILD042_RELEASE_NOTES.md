## NRHIS Sprint 2 — Build042

Build042 removes the final recurring keyboard interruption from the automated lifecycle.

### Included

- Noninteractive handling of Git for Windows locked-directory deletion retries
- Automatic `n` response without pruning or destructive cleanup
- Visible logging of skipped deletion messages
- Existing transient Git retry retained
- Fail-closed behavior for genuine Git failures
- Full one-step discovery, extraction reuse, PR/merge, pre-release publication, authenticated verification, completion receipt, installer archival, and next-build chaining retained

This release is a pre-release and is not the latest production release.
