# Build077 — Generic Completion Helper

Build077 installs `scripts/release/Complete-NrhisBuild.ps1` as the canonical release-completion helper used by per-build wrappers. The helper publishes or reuses the GitHub prerelease, writes the build completion receipt, and preserves the resumable lifecycle contract.
