## Summary

- adds a self-contained build runner independent of deleted or branch-specific lifecycle helpers;
- retries temporary CI-registration gaps;
- blocks merge on failed checks;
- publishes prereleases and completion receipts;
- adds local ZIP and SHA-256 generation for the next build.
