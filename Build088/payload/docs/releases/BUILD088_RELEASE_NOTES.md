# NRHIS Sprint 2 Build088 Release Notes

Build088 establishes a self-contained one-step lifecycle. It removes dependency on legacy completion scripts, starts each build from the current remote base branch, waits for CI registration, merges only after successful checks, publishes the prerelease, creates completion receipts, and provides local next-build ZIP generation.
