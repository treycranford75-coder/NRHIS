# Build064 Release Notes

Adds twice-daily Windows scheduling and auditable run monitoring for NRHIS operations. The scheduler defaults to QA pass count zero and therefore does not authorize public release without the separate two-pass verification workflow.
