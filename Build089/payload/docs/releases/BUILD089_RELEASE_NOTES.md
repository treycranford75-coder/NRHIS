# NRHIS Sprint 2 Build089 Release Notes

Build089 stabilizes the one-step release lifecycle. It targets `develop`, uses explicit staging rather than `git add -A`, prevents generated hydrologic runtime data and oversized files from entering release commits, avoids editor prompts, provides visible validation output, manages the PR and merge, publishes the prerelease, writes completion receipts, and creates a verified installer archive and SHA-256 checksum.
