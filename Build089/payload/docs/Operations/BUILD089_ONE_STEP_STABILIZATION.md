# Build089 One-Step Stabilization

Build089 is limited to release-workflow stabilization. It uses `develop` as the base branch, stages an explicit allowlist, rejects generated runtime data and oversized blobs, performs noninteractive commits, manages the pull request and checks, publishes the prerelease, writes receipts, and creates a verified installer archive.
