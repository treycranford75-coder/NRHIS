# Build078 Verified Receipt Contract

Build078 aligns the canonical completion helper with the installer archive gate. Completion receipts now contain both `status: verified` and `verified: true`. The GitHub release lookup also treats a missing release as a normal create-path condition, including when PowerShell native-command errors are enabled.
