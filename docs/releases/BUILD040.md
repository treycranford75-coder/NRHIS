# Build040

Build040 removes the remaining friction from the one-step lifecycle through expanded package discovery, verified root copying, resumable extraction, whitespace-clean payloads, and noninteractive transient-lock handling.
